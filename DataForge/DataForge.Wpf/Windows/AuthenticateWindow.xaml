<Window x:Class="DataForge.Wpf.Windows.AuthenticateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataForge.Wpf.Windows"
        mc:Ignorable="d"
        xmlns:mv="clr-namespace:DataForge.Wpf.ViewModels"
        Title="AuthenticateWindow" Height="450" Width="800">

    <Window.DataContext>
        <mv:AuthenticateWindowVM/>
    </Window.DataContext>

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBox Margin="10"
                     ToolTip="Username TextBox"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, NotifyOnValidationError=True, ValidatesOnDataErrors=True, ValidatesOnNotifyDataErrors=True}"/>
            <TextBox Margin="10"
                     ToolTip="Password Textbox"
                     Text="{Binding Password, UpdateSourceTrigger=PropertyChanged, NotifyOnValidationError=True, ValidatesOnDataErrors=True, ValidatesOnNotifyDataErrors=True}"/>
        </StackPanel>
    </Grid>
</Window>
